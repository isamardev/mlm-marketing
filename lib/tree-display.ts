/**
 * How deep sponsor-chain queries go for **tree / lists / level counts** (UI + stats).
 * This is **not** the commission cap — that stays 20 levels in `lib/mlm-logic.ts`.
 */
export const TREE_QUERY_MAX_DEPTH = 100;
